# AGENTS.md

## 协作规则

- 完成代码或文档变更后，立即创建 git 提交并推送到当前分支。
- 提交信息使用 gitmoji 规范，例如 `✨ feat: add credential api`、`📝 docs: update readme`。
- 提交前检查 `git status`、`git diff` 和最近提交，确认只提交本次相关文件。
- 不要回滚或覆盖用户已有改动，除非用户明确要求。
